# A Telegram bot: EXCHANGE RATES

## You can find out exchange rates from Privatbank and Monobank

Initiate clone project

Init dependencies:

`npm i`

Create `.env` file and add:

`TELEGRAM_BOT_TOKEN=<bot-token>`

### Start server:

`npm start`
