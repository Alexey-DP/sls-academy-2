## A CLI application is used to create multiple users, put their information, and then search for users in an already populated database.

Initiate clone project

Init dependencies:

`npm i`

Start app with:

`npm start`
